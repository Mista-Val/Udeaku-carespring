apiVersion: v1
kind: Secret
metadata:
  name: udeaku-secrets
  namespace: udeaku-carespring
  labels:
    app: udeaku-carespring
type: Opaque
data:
  # Base64 encoded values
  # To encode: echo -n 'your-value' | base64
  GOOGLE_MAPS_API_KEY: ""  # Add if needed
  EMAIL_PASSWORD: ""        # Add if needed for email service
  DATABASE_URL: ""         # Add if using database
