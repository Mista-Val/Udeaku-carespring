apiVersion: apps/v1
kind: Deployment
metadata:
  name: udeaku-carespring
spec:
  template:
    spec:
      containers:
      - name: udeaku-carespring
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
